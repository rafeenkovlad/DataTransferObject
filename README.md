# DataTransferObject
DataTransferObject allows creating DTOs on the fly from request data based on a predefined parameter list. Properties that are not declared in the DTO are automatically skipped. Nested DTOs and DTO collections are supported.
